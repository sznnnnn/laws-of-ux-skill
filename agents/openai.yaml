interface:
  display_name: "Laws of UX + Design Spec"
  short_description: "UX laws, BuddyUp design spec, and governance checklists"
  default_prompt: "Use $laws-of-ux-2 to review this interface against UX laws and the BuddyUp product design spec. List P0–P3 findings and concrete fixes."

policy:
  allow_implicit_invocation: true
